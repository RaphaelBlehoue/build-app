import { When, Then } from 'cucumber';

When('the client creates a POST request to \/users', function () {
  return 'pending';
});

When('attaches a generic empty payload', function () {
  return 'pending';
});

When('send the request', function () {
  return 'pending';
});

Then('our API should response with a {int} HTTP status code', function (int) {
  return 'pending';
});

Then('the payload of response should be a JSON object', function () {
  return 'pending';
});

Then('contains a message property which says {string}', function (string) {
  return 'pending';
});
